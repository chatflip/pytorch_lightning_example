# デフォルトトレーナー設定
trainer:
  max_epochs: 100
  accelerator: "gpu"
  devices: 1
  precision: "16-mixed"
  strategy: "auto"
  deterministic: true
  log_every_n_steps: 10
  val_check_interval: 1.0

checkpoint:
  monitor: "val_loss"
  mode: "min"
  save_top_k: 3
  save_last: true

